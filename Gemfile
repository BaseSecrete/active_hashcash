source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in active_hashcash.gemspec.
gemspec

gem "puma"

gem "sqlite3"

gem "sprockets-rails" # TODO: Try to remove this dependency
